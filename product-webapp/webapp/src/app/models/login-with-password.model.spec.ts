import { LoginWithPassword } from './login-with-password.model';

describe('LoginWithPassword', () => {
  it('should create an instance', () => {
    expect(new LoginWithPassword()).toBeTruthy();
  });
});

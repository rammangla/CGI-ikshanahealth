import { CaretakerAppointments } from './caretaker-appointments.model';

describe('CaretakerAppointments', () => {
  it('should create an instance', () => {
    expect(new CaretakerAppointments()).toBeTruthy();
  });
});

import { DoctorAppointments } from './doctor-appointments.model';

describe('DoctorAppointments', () => {
  it('should create an instance', () => {
    expect(new DoctorAppointments()).toBeTruthy();
  });
});

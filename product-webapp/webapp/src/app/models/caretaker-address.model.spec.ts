import { CaretakerAddress } from './caretaker-address.model';

describe('CaretakerAddress', () => {
  it('should create an instance', () => {
    expect(new CaretakerAddress()).toBeTruthy();
  });
});
